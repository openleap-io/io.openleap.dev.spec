openapi: 3.1.0
info:
  title: Param Configuration Service
  version: 1.0.0
  description: |
    Runtime configuration store for global and tenant-scoped entries.
    
    Full endpoint specification derived from domain-specs/param_cfg-spec.md §6.
  contact:
    name: team-param
    email: platform-core@openleap.io
  x-owner: team-param
  x-suite: param
  x-domain: cfg

servers:
  - url: http://localhost:8102
    description: Local development
  - url: https://api.openleap.io
    description: Production

paths:
  /api/param/cfg/v1:
    get:
      summary: Service root / health
      operationId: getServiceInfo
      tags: [health]
      responses:
        '200':
          description: Service info

  # Full paths defined in domain-specs/param_cfg-spec.md §6.
  # This stub provides the OpenAPI metadata contract per blueprint §2.3.1.
  # Implementation-complete OpenAPI MUST be generated from the domain spec.

security:
  - bearerAuth: []

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
