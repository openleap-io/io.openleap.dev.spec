openapi: 3.1.0
info:
  title: Param SI Unit Service
  version: 1.0.0
  description: |
    SI unit definitions, prefixes, and dimensional-analysis-based conversion.
    
    Full endpoint specification derived from domain-specs/param_si-spec.md §6.
  contact:
    name: team-param
    email: platform-core@openleap.io
  x-owner: team-param
  x-suite: param
  x-domain: si

servers:
  - url: http://localhost:8103
    description: Local development
  - url: https://api.openleap.io
    description: Production

paths:
  /api/param/si/v1:
    get:
      summary: Service root / health
      operationId: getServiceInfo
      tags: [health]
      responses:
        '200':
          description: Service info

  # Full paths defined in domain-specs/param_si-spec.md §6.
  # This stub provides the OpenAPI metadata contract per blueprint §2.3.1.
  # Implementation-complete OpenAPI MUST be generated from the domain spec.

security:
  - bearerAuth: []

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
