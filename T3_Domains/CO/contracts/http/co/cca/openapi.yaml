openapi: 3.1.0
info:
  title: CO Cost Center Accounting Service
  version: 1.0.0
  description: |
    Cost Center Accounting (CCA) domain service for the Controlling suite.
    Manages cost centers, cost elements, and cost postings by organizational unit.

    Full endpoint specification derived from domain-specs/co_cca-spec.md §6.
  contact:
    name: team-co
    email: co-team@openleap.io
  x-owner: team-co
  x-suite: co
  x-domain: cca

servers:
  - url: http://localhost:TBD
    description: Local development
  - url: https://api.openleap.io
    description: Production

paths:
  /api/co/cca/v1:
    get:
      summary: Service root / health
      operationId: getServiceInfo
      tags: [health]
      responses:
        '200':
          description: Service info

  # Full paths defined in domain-specs/co_cca-spec.md §6.
  # This stub provides the OpenAPI metadata contract per blueprint §2.3.1.
  # Implementation-complete OpenAPI MUST be generated from the domain spec.

security:
  - bearerAuth: []

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
