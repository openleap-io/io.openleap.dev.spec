openapi: 3.1.0
info:
  title: CO Management Reporting Service
  version: 1.0.0
  description: |
    Management Reporting (RPT) domain service for the Controlling suite.
    Generates and distributes management reports including cost center reports,
    profit center reports, profitability statements, and variance reports.

    Full endpoint specification derived from domain-specs/co_rpt-spec.md §6.
  contact:
    name: team-co
    email: co-team@openleap.io
  x-owner: team-co
  x-suite: co
  x-domain: rpt

servers:
  - url: http://localhost:TBD
    description: Local development
  - url: https://api.openleap.io
    description: Production

paths:
  /api/co/rpt/v1:
    get:
      summary: Service root / health
      operationId: getServiceInfo
      tags: [health]
      responses:
        '200':
          description: Service info

  # Full paths defined in domain-specs/co_rpt-spec.md §6.
  # This stub provides the OpenAPI metadata contract per blueprint §2.3.1.
  # Implementation-complete OpenAPI MUST be generated from the domain spec.

security:
  - bearerAuth: []

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
