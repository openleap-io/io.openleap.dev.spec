openapi: 3.1.0
info:
  title: CO Profit Center Accounting Service
  version: 1.0.0
  description: |
    Profit Center Accounting (PCA) domain service for the Controlling suite.
    Manages profit center master data, profit center assignments to cost objects,
    and provides profit center balance reporting.

    Full endpoint specification derived from domain-specs/co_pca-spec.md §6.
  contact:
    name: team-co
    email: co-team@openleap.io
  x-owner: team-co
  x-suite: co
  x-domain: pca

servers:
  - url: http://localhost:TBD
    description: Local development
  - url: https://api.openleap.io
    description: Production

paths:
  /api/co/pca/v1:
    get:
      summary: Service root / health
      operationId: getServiceInfo
      tags: [health]
      responses:
        '200':
          description: Service info

  # Full paths defined in domain-specs/co_pca-spec.md §6.
  # This stub provides the OpenAPI metadata contract per blueprint §2.3.1.
  # Implementation-complete OpenAPI MUST be generated from the domain spec.

security:
  - bearerAuth: []

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
