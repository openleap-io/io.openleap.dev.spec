openapi: 3.1.0
info:
  title: PPS MES Service
  version: 1.0.0
  description: |
    Manufacturing Execution Service for production order and work order management.

    Full endpoint specification derived from domain-specs/pps_mes-spec.md §6.
  contact:
    name: team-pps
    email: pps-team@openleap.io
  x-owner: team-pps
  x-suite: pps
  x-domain: mes

servers:
  - url: http://localhost:TBD
    description: Local development
  - url: https://api.openleap.io
    description: Production

paths:
  /api/pps/mes/v1:
    get:
      summary: Service root / health
      operationId: getServiceInfo
      tags: [health]
      responses:
        '200':
          description: Service info

  # Full paths defined in domain-specs/pps_mes-spec.md §6.
  # This stub provides the OpenAPI metadata contract per blueprint §2.3.1.
  # Implementation-complete OpenAPI MUST be generated from the domain spec.

security:
  - bearerAuth: []

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
