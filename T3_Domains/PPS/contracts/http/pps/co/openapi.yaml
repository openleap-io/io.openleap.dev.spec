openapi: 3.1.0
info:
  title: PPS CO Service
  version: 1.0.0
  description: |
    Controlling service for production cost collector management.

    Full endpoint specification derived from domain-specs/pps_co-spec.md §6.
  contact:
    name: team-pps
    email: pps-team@openleap.io
  x-owner: team-pps
  x-suite: pps
  x-domain: co

servers:
  - url: http://localhost:TBD
    description: Local development
  - url: https://api.openleap.io
    description: Production

paths:
  /api/pps/co/v1:
    get:
      summary: Service root / health
      operationId: getServiceInfo
      tags: [health]
      responses:
        '200':
          description: Service info

  # Full paths defined in domain-specs/pps_co-spec.md §6.
  # This stub provides the OpenAPI metadata contract per blueprint §2.3.1.
  # Implementation-complete OpenAPI MUST be generated from the domain spec.

security:
  - bearerAuth: []

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
