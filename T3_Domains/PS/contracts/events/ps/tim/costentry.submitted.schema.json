{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.openleap.io/ps/tim/costentry.submitted.schema.json",
  "title": "ps.tim.costentry.submitted",
  "description": "Cost entry submitted for approval",
  "type": "object",
  "required": [
    "eventId",
    "eventType",
    "timestamp",
    "tenantId",
    "correlationId",
    "producer",
    "schemaVersion",
    "payload"
  ],
  "properties": {
    "eventId": {
      "type": "string",
      "format": "uuid",
      "description": "Unique event identifier"
    },
    "eventType": {
      "type": "string",
      "const": "ps.tim.costentry.submitted",
      "description": "Event routing key"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 event timestamp"
    },
    "tenantId": {
      "type": "string",
      "description": "Tenant identifier"
    },
    "correlationId": {
      "type": "string",
      "format": "uuid",
      "description": "Correlation ID for tracing"
    },
    "causationId": {
      "type": "string",
      "format": "uuid",
      "description": "ID of the causing command/event"
    },
    "producer": {
      "type": "string",
      "const": "ps-tim-svc",
      "description": "Producing service"
    },
    "schemaVersion": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Schema version (semver)"
    },
    "payload": {
      "type": "object",
      "description": "Event-specific payload for ps.tim.costentry.submitted",
      "properties": {
        "costentryId": {
          "type": "string",
          "format": "uuid",
          "description": "Identifier of the submitted cost entry"
        },
        "submittedBy": {
          "type": "string",
          "description": "Principal ID of the user who submitted the cost entry"
        }
      },
      "required": [
        "costentryId",
        "submittedBy"
      ]
    }
  },
  "additionalProperties": false
}
