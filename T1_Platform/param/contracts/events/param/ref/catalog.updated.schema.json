{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "param.ref.catalog.updated",
  "title": "param.ref.catalog.updated",
  "description": "Event published when catalog is updated",
  "type": "object",
  "required": ["eventId", "eventType", "timestamp", "tenantId", "correlationId", "producer", "schemaVersion", "payload"],
  "properties": {
    "eventId": { "type": "string", "format": "uuid" },
    "eventType": { "type": "string", "const": "param.ref.catalog.updated" },
    "timestamp": { "type": "string", "format": "date-time" },
    "tenantId": { "type": "string", "format": "uuid" },
    "correlationId": { "type": "string", "format": "uuid" },
    "causationId": { "type": "string", "format": "uuid" },
    "producer": { "type": "string", "const": "param--svc" },
    "schemaVersion": { "type": "string", "const": "1.0.0" },
    "payload": {
      "type": "object",
      "required": [],
      "properties": {
        "catalogId": { "type": "string" },
        "changedCodes": { "type": "array", "items": { "type": "string" } }
      }
    }
  }
}
