{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.openleap.io/iam/tenant/tenant.created.schema.json",
  "title": "iam.tenant.tenant.created",
  "description": "New tenant provisioned",
  "type": "object",
  "required": [
    "eventId",
    "eventType",
    "timestamp",
    "tenantId",
    "correlationId",
    "producer",
    "schemaVersion",
    "payload"
  ],
  "properties": {
    "eventId": {
      "type": "string",
      "format": "uuid",
      "description": "Unique event identifier"
    },
    "eventType": {
      "type": "string",
      "const": "iam.tenant.tenant.created",
      "description": "Event type routing key"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 event timestamp"
    },
    "tenantId": {
      "type": "string",
      "format": "uuid",
      "description": "Tenant context"
    },
    "correlationId": {
      "type": "string",
      "format": "uuid",
      "description": "Correlation ID for tracing"
    },
    "causationId": {
      "type": "string",
      "format": "uuid",
      "description": "ID of the causing event or command"
    },
    "producer": {
      "type": "string",
      "const": "iam-tenant-svc",
      "description": "Producing service"
    },
    "schemaVersion": {
      "type": "string",
      "const": "1.0.0",
      "description": "Schema version"
    },
    "payload": {
      "type": "object",
      "description": "Event-specific payload for iam.tenant.tenant.created",
      "properties": {
        "tenantId": {
          "type": "string",
          "format": "uuid",
          "description": "Affected tenant identifier"
        },
        "principalId": {
          "type": "string",
          "format": "uuid",
          "description": "Acting principal"
        }
      },
      "required": [
        "tenantId"
      ]
    }
  }
}